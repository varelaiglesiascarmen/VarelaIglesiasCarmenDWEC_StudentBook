// Tarea 3: Iteración sobre arrays
let nombres = ["Carmen","Reyes","Mercedes","Ana","Lucia"];

// Usando un bucle for recorre el array e imprime los nombres
for (let i = 0; i < nombres.length; i++) {
    console.log(nombres[i]);
}

// Usando un bucle forEach recorre el array e imprime los nombres
nombres.forEach(item=> console.log(item));